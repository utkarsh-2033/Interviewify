import React from "react";
import { SignIn } from "@clerk/clerk-react";

const Login = () => {
  return (
    <SignIn
      // where to send user after successful sign in
    />
  );
};

export default Login;
